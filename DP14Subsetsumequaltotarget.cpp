#include <bits/stdc++.h>
using namespace std;

bool subsetSumToK(int n, int k, vector<int> &arr) {
    sort(arr.begin(),arr.end());
    for (int i = 0 ; i < n && arr[i] < k ; i++)
}

int main()
{
    
    return 0;
}